#!/bin/bash

echo "I am saving your secrets..."

DFDIR="$HOME/.dotfiles"
. "$DFDIR/.bashrc.fns"

$(
    cd $DFDIR
    tar cvf secrets.tar secrets
    gpg -ca --force-mdc secrets.tar
    rm -f secrets.tar
    sha512sum secrets.tar.asc > secrets.tar.asc.sha512sum
)
